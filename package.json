{
  "devDependencies": {
    "lint-staged": "^13.0.3"
  },
  "lint-staged": {
    "./**/*.rb": [
      "bundle exec rubocop -a"
    ],
    "{Gemfile,Rakefile,config.ru}": [
      "bundle exec rubocop -a"
    ]
  },
  "scripts": {
    "prepare": "husky install"
  }
}
