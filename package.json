{
  "name": "offinso",
  "description": "a really cool app",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "lint-staged": "^13.0.3"
  },
  "lint-staged": {
    "./**/*.rb": [
      "bundle exec rubocop -a"
    ],
    "{Gemfile,Rakefile,config.ru}": [
      "bundle exec rubocop -a"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "precommit": "bundle exec rubocop -a",
    "prepush": "bundle exec rubocop -a"
  }
}
